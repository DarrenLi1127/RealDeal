import Catalog from '../catalog/Catalog.tsx';

const Dashboard = () => {
    return (
        <main className="dashboard-container">
            {/* future: greeting, summary, etc. */}
            <Catalog />
        </main>
    );
};

export default Dashboard;
